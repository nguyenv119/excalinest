// ─── Layout Constants ────────────────────────────────────────────────────────
// Shared sizing constants used by the organize-canvas skill and layout tools.
// These values define the default dimensions and spacing for canvas nodes.

/** Default width of a leaf (child) node in pixels. */
export const LEAF_W = 205;

/** Default height of a leaf (child) node in pixels. */
export const LEAF_H = 52;

/** Horizontal gap between sibling nodes in pixels. */
export const H_GAP = 14;

/** Vertical gap between rows of child nodes in pixels. */
export const V_GAP = 12;

/** Padding inside a parent node container in pixels. */
export const PAD = 18;

/** Height reserved for the parent node title bar in pixels. */
export const TITLE_H = 48;
